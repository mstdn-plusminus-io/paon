# Mastodon 4.5.15 compatibility evidence

Generated on 2026-08-13 (Asia/Tokyo) from the Paon 4.5 compatibility
checkout. This document records source compatibility and automated gate
evidence separately from production admission. The checklist in
`docs/todo-4.5.15.md` is the research and traceability input; a checked source
item or a passing local gate is not, by itself, a production-readiness claim.

## Implementation disposition

| Area                                                   | TODO IDs                                                                                     | Implementation disposition                                           | Verification                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------ | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PostgreSQL and migration                               | `DB45-01` through `DB45-04`                                                                  | Implemented and exercised through the automated database gates       | The final `20251023210145` schema, reviewed 4.4-to-4.5 expand/backfill/validate/contract path, data invariants, and compatibility paths from empty, 4.4.22, 4.3.23, and 4.2.19 databases are covered. Production-volume migration rehearsal remains an admission item.                                                                                                        |
| Official Quote authoring and lifecycle                 | `QUOTE45-01` through `QUOTE45-04`                                                            | Implemented using the official Mastodon PostgreSQL/FEP-044f contract | REST creation, scheduled publication, policy and authorization lifecycle, counters, notifications, mail, push, streaming, search, moderation display, and outgoing/incoming federation use the official SQL quote state. The former DynamoDB extension is not a runtime fallback or dual-write path.                                                                          |
| Replies and ActivityPub                                | `REPLY45-01`, `AP45-01` through `AP45-03`                                                    | Implemented                                                          | Bounded reply refresh, FEP-7888 conversation collections, numeric local actor identifiers, legacy username identifier compatibility, and converted-object Update handling have focused Go and route coverage. Live remote-server interoperability remains pending.                                                                                                            |
| Feeds, landing page, registration, settings, and admin | `FEED45-01`, `WEB45-01`, `REG45-01` through `REG45-03`, `PREF45-01`                          | Implemented                                                          | Granular live/topic feed access, configurable landing page, username blocks, normalized antispam sets, denied-registration sign-in messaging, posting defaults, admin pages, and permission boundaries are included in the server and frontend regression suites.                                                                                                             |
| Security and patch releases                            | `SEC45-01` through `SEC45-04`, releases `4.5.1` through `4.5.15`                             | Implemented in the applicable Go execution paths                     | Federation/fetch restrictions, legacy and RFC 9421 signatures, Web Push ownership and invalid subscriptions, e-mail validation, LDAP TLS isolation, attachment limits, and release-specific negative fixtures are covered by the source and Go regression gates. Exact-image results are recorded below; unresolved findings remain a production risk decision.               |
| Jobs, FASP, search, push, media, and runtime           | `JOB45-01`, `JOB45-02`, `SEARCH45-01`, `PUSH45-01`, `MEDIA45-01`, `RUNTIME45-01`, `SEO45-01` | Implemented                                                          | FASP delivery cooldown, follower synchronization digest checks, search behavior, notification lifecycle, cursor-safe vacuum processing, storage/media paths, schema.org metadata, runtime floors, and process-role behavior have focused or external-service coverage.                                                                                                        |
| Existing-design React UI and i18n                      | `UI45-01` through `UI45-04` and the UI changelog audit                                       | Implemented without importing the Mastodon 4.5 layout                | Paon's existing status, composer, timeline, settings, admin, theme, emotional/custom-spoiler, and single-column-chat design is extended with official Quote, reply refresh, feed access, landing, Twemoji 16, emoji mode, viewport, keyboard, and accessibility semantics. Automated frontend gates pass; human browser, accessibility, and visual acceptance remain pending. |
| Advertised identity                                    | `RUNTIME45-01`                                                                               | Implemented                                                          | Paon now reports Paon `7.3.0`, Mastodon compatibility `4.5.15`, and Mastodon API compatibility level `7`. This software identity is not a claim that production admission has completed.                                                                                                                                                                                      |

## Official Quote runtime boundary

Mastodon 4.5 makes Quote authoring and outgoing federation part of the official
contract. Paon therefore uses the official PostgreSQL `quotes` model, REST
entities and actions, FEP-044f activities, notifications, streaming, search,
and retained-design composer as its only live Quote implementation.

The former Paon DynamoDB Quote extension is offline during normal operation.
Its only supported use is the existing resumable, read-only-input,
one-way `paon-admin quotes cutover` into the official PostgreSQL model. The web,
worker, REST, serializer, ActivityPub, streaming, search, and composer paths do
not fall back to DynamoDB, write new DynamoDB Quote state, or dual-write. Any
eventual DynamoDB retirement is a separate explicit operator action.

## Executed compatibility gates

After implementation, the source audit re-read the v4.5.0 through v4.5.15
change inventory and the v4.5.15 target tree, and mapped every applicable item
to a Paon Go/Rspack/Asynq path or an explicit non-applicable upstream-only
path. The final static source audit contains **0 concrete implementation
gaps**. This static result does not substitute for the live, human, or
production-topology gates listed below.

| Gate                                          | Result                              | Evidence                                                                                                                                                                                                                                                                                                  |
| --------------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Go regression                                 | PASS                                | `task test:rtk`: 3,555 tests across 23 packages in a listener-capable environment after the final runtime-gap fix.                                                                                                                                                                                        |
| Go build and static checks                    | PASS                                | `task build` produced all five commands. `go vet ./...`, `go mod verify`, `go mod tidy -diff`, the all-Go-file `gofmt -d` check, and `git diff --check` pass.                                                                                                                                             |
| Official route parity                         | PASS                                | `task routes:audit`: 739 mapped, 0 accepted exclusions, 0 unmapped, and 692 intentional Go-only routes. The Mastodon 4.5.15 Rails route manifest SHA-256 is `d64f916a8815adb741442ae3be22b2aea896a1aa6132a0a1f0468f50272cc3cd`.                                                                           |
| Official PostgreSQL catalog static comparison | PASS                                | The target comparison records 107 tables, 921 columns, 195 table indexes plus 5 view indexes, 143 foreign keys, and all 554 migration markers. Table, column, index, and foreign-key name differences are 0, and the official username-block seed contains 23 entries.                                    |
| PostgreSQL 14 and Redis 7 integration         | PASS                                | The five integration-test packages pass against real PostgreSQL 14 and Redis 7, including empty-database initialization and populated 4.4.22, 4.3.23, and 4.2.19 upgrade paths.                                                                                                                           |
| External services                             | PASS                                | The external gate passes against Meilisearch, S3-compatible storage, SMTP, FFmpeg/ffprobe, and libvips; measured suite time is 12.239 seconds.                                                                                                                                                            |
| Frontend static and automated gates           | PASS                                | 85 Jest suites, 265 tests, and 26 snapshots pass. JavaScript lint has 0 errors and 7 existing warnings; TypeScript checking and the production Rspack build pass. Extracted messages contain 960 English keys and 1,095 Japanese keys, with 0 English-only keys.                                          |
| Compose configuration                         | PASS                                | `docker compose config` passes.                                                                                                                                                                                                                                                                           |
| Exact candidate image                         | PASS                                | Application tree `06338886916f66e49e31a00fdbbd12801d14a491` produced image `sha256:49ff79d4b0db820be44f775629b228b0c0fc732af8bd45a3f32f8a1fab695333` (`linux/amd64`, 1,256,956,313 bytes). The tree fingerprint was unchanged before and after the build.                                                 |
| Exact-image roles and streaming               | PASS                                | Fresh migration and `--check-config`, isolated web/worker/all roles, worker graceful restart/readiness, real retry exhaustion to archive, authenticated SSE, and authenticated WebSocket pass. The final worker/all logs contain no obsolete `imports` query.                                             |
| Dependency and image security                 | PASS with production review pending | Reachable Go vulnerabilities: 0; frontend high/critical: 0; CVE-2026-8461: fixed in the installed Debian FFmpeg package; fixable container high/critical: 0. Trivy's complete inventory still contains 211 unfixed Debian high/critical rows, and frontend audit contains 1 moderate build-chain finding. |

The route and catalog comparisons are static target-tree evidence. They do not
establish response-level equivalence with a running official Mastodon server,
remote federation interoperability, or safe production migration behavior.

## Exact image and dependency boundary

The exact-image build, scan, dependency, process-role, SSE, and WebSocket
results are recorded in
[`4.5.15-dependency-security.md`](4.5.15-dependency-security.md). The identified
local candidate has clean reachable-Go, frontend high/critical,
CVE-2026-8461, nested-media-network, and fixable-container high/critical gates.
It is not an immutable published release and is not production-admitted by
those results. Release owners must still disposition the one moderate frontend
build-chain finding, the non-reachable but fixable NTLM dependency, and all 211
unfixed Debian high/critical rows. Any source, lock, base-image, or promoted
image change requires a rebuild and rescan.

## Production admission still required

This checkout has automated compatibility evidence, but it is **not declared
production ready**. The following gates remain pending and require a live
reference, production-like infrastructure, or human evidence:

- run public, authenticated, owner, and admin response differentials against a
  live official Mastodon 4.5.15 Rails deployment;
- exercise official Quote/FEP-044f, FEP-7888, numeric actor identifiers, RFC
  9421 and legacy signatures, reply fetching, follower synchronization, and
  lifecycle edge cases with live remote Mastodon and other federated servers;
- complete desktop/mobile/touch/keyboard/screen-reader/reduced-motion testing
  and retained-theme visual regression in real browsers, including the Quote
  composer, nested status states, reply refresh, feeds, settings, and admin
  flows;
- rehearse the staged migration and rollback on production-volume data,
  measuring locks, table rewrites, replica lag, disk headroom, backup/restore,
  old/new writer fences, and the destructive-contract acknowledgement;
- rehearse the deployed PostgreSQL/Redis topology, Redis or compatible-service
  failover, web/worker rolling restart, storage and Meilisearch failures, mail,
  FASP, media processing, and queue recovery under production-like load; and
- complete the explicit dependency/image risk disposition and rebuild/rescan if
  the promoted image, base packages, dependency lock, or application tree
  changes.

Production admission is a separate decision after these items are evidenced;
the `7.3.0` / `4.5.15` / API `7` identity does not waive them.
